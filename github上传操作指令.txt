上传内容：
	1.git pull		获取当前github库最新内容（类似于更新本地库，必须要做否则可能会出现上传错误）
	2.git add 文件名 . 属性 	上传第一步骤 单个文件
	  其他方法：
	 	git add -A 		上传全部更改内容 （把本地库的所有内容都上传，更改github库中内容，谨慎使用）
	3.git commint -m“注释”	上传的第二步骤 添加注释
	4.git push			上传的第三步骤 确认上传，此步骤必不可少，它是上传的最后一步，完成即为真正上传成功

 